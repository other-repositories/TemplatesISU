from  src.abstract_reference import abstract_reference

class nom_group_model(abstract_reference):
    def __init__(self, name: str = None):
        super().__init__(name)

    def create_group():
        item = nom_group_model("Ингредиенты")
        return item